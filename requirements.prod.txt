# Production dependencies (for Railway deployment)
# Excludes dev/notebook dependencies to keep Docker image small

# Core
pandas==2.2.2
numpy==1.26.4

# Data loading (required for import, uses cache in prod)
datasets==2.19.0

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.1

# LLM providers
openai==1.30.1
groq>=0.11.0

# Fuzzy matching
fuzzywuzzy==0.18.0
python-Levenshtein==0.25.1

# Utilities
tiktoken==0.7.0

# API Server
fastapi==0.111.0
uvicorn[standard]==0.30.1
